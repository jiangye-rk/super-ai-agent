spring:
  application:
    name: jiang-ai-agent
  datasource:
    mysql:
      url: jdbc:mysql://localhost:3306/jiang_ai_agent?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
      username: root
      password: 123456
      driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 300000
      max-lifetime: 1200000
      connection-timeout: 20000
      pool-name: HikariPool-ChatMemory
    postgresql:
      url: jdbc:postgresql://pgm-uf6c34ryjja77dhgvo.rwlb.rds.aliyuncs.com:5432/ye_ai_agent
      username: ye_ai_agent
      password: Jrk123456

  ai:
    dashscope:
      api-key: sk-68d928c9d1da4e31acb220498eceaf6b
      chat:
        options:
          model: qwen-plus
      embedding:
        options:
          model: text-embedding-v4
    vectorstore:
      pgvector:
        index-type: HNSW
        distance-type: COSINE_DISTANCE
        max-document-batch-size: 10

mybatis:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mapper/**/*.xml

logging:
  level:
    com.jiangye.jiangaiagent.chatmemory: DEBUG
    org.springframework.ai: DEBUG
